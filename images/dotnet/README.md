# dev-cli-dotnet (planned)

A .NET-focused variant of the shared dev CLI base image is planned for a future release.

It will reuse the `base` stage from [`docker/Dockerfile`](../../docker/Dockerfile) and add the .NET SDK and related tooling. Until then, use the Python image at [`../python/`](../python/).
